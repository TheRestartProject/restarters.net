default:
    extensions:
        Laracasts\Behat: 
            env_path: .env.behat
        Behat\MinkExtension:
            #default_session: laravel
            base_url: "http://127.0.0.1:8000"
            #laravel: ~
            show_cmd: firefox.exe %s
            sessions:
              first_session:
                goutte: ~
    
    suites:
        default:
            contexts:
                - FeatureContext
                # - OnboardingContext
